// ProfileButton removed: component kept as an inert stub to avoid breaking imports.
// The UI panel was intentionally removed from the layout. If you want to
// fully delete this file later, we can remove it once no imports remain.

export default function ProfileButton() {
  return null;
}
